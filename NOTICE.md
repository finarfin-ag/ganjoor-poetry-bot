# Data provenance and attribution

This project is designed to ingest the public export from `ganjoor/ganjoor-data`.

The source corpus and this project's software are separate concerns. Before redistributing generated corpus artifacts, verify the licensing and attribution requirements of the exact upstream snapshot being used.

The importer records upstream identifiers and paths so generated results can be traced back to Ganjoor.
